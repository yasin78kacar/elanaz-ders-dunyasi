/** Typo-tolerant re-export — canonical: `src/contexts/GamificationContext.tsx` */
export {
  GamificationProvider,
  GamificationProvider as GameificationProvider,
  useGamification,
  useGameification,
} from '../contexts/GamificationContext';
